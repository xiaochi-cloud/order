spring:
  application:
    name: order
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config
      profile: test
#    stream:
#      bindings:
#        myMessage: order
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    guest: guest
  redis:
    host: localhost
    port: 6379